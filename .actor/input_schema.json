{
    "title": "Hellotickets Scraper",
    "description": "Scrape tours, attraction tickets, and city passes from Hellotickets category pages. Provide a Hellotickets listing URL and collect structured travel data at scale.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrl": {
            "title": "Start URL",
            "type": "string",
            "description": "Any public Hellotickets city, category, event, venue, performer, or search URL.",
            "editor": "textfield",
            "prefill": "https://www.hellotickets.com/us/new-york/c-1?qs=New%20York"
        },
        "results_wanted": {
            "title": "Maximum number of items",
            "type": "integer",
            "description": "Maximum number of unique ticket or tour listings to save.",
            "minimum": 1,
            "default": 20,
            "prefill": 20,
            "editor": "number"
        },
        "proxyConfiguration": {
            "title": "Proxy configuration",
            "type": "object",
            "description": "Optional Apify or custom proxy configuration for protected site requests.",
            "editor": "proxy",
            "default": {},
            "prefill": {
                "useApifyProxy": false
            }
        }
    },
    "required": [],
    "additionalProperties": false
}
